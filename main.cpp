#include <iostream>
#include <sstream>
#include <string>
#include <vector>

#include "LLR.h"
using namespace std;

int main(int argc, char* argv[]) {
  // for (int i = 0; i < argc; i++) cout << argv[i] << endl;
  const string& fn = argv[1];
  // vector<command*> cmds = commands(fn);
  // pass_1(cmds, symbolTable, length);
  // pass_2(cmds, symbolTable, length);
}
